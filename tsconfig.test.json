{
  "// ": "The Vitest suite. It exercises both sides, so it needs Node globals and DOM types together — which is exactly why it is separate from the two source configs, where mixing them would let server code reach for `document`.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "// include": "src/shared is listed explicitly because it carries the `declare global` that adds `window.desktopApi`. Modules reached only through an import chain do not contribute global augmentations unless the declaring file is itself part of the program, so a renderer test that touches window.desktopApi would otherwise fail here while compiling cleanly under tsconfig.web.json.",
  "include": ["tests/**/*.ts", "src/shared/**/*.ts", "vitest.config.mts"]
}
